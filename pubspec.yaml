name: timer_app
description: "A minimal timer app built as a side project, focused purely on experimenting with UI theme changes. It lets you switch between light, dark, and custom themes while running a simple timer. The goal isn’t functionality, but exploring design aesthetics and smooth theme transitions."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true